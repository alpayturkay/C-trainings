//C program that calculates the sum of even integers between 1 and  n using a do…while loop.
#include<stdio.h>

int main(){
int n,i=1,sum=0;
do{
i=i+2;
sum=sum+i;
scanf("%d",&n);
printf("Sum is %d",sum);

}while(i<n);

return 0;
}
